framework:
    rate_limiter:
        login:
            policy: sliding_window
            limit: 5
            interval: '15 minutes'
        api_default:
            policy: token_bucket
            limit: 60
            interval: '1 minute'
